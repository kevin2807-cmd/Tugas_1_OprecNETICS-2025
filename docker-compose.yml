version: "3.8"

services:
  api:
    image: ${DOCKERHUB_USERNAME}/express-api:latest
    container_name: express-api
    restart: always
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
